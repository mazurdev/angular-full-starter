import {faArrowDown, faCookie, faTimes} from '@fortawesome/free-solid-svg-icons';

export class ICONS {
  public static readonly iconCookie = faCookie;
  public static readonly iconArrowDown = faArrowDown;
  public static readonly iconTimes = faTimes;
}
